SITE_PASSWORD=change-this-password
SITE_AUTH_SECRET=replace-with-a-long-random-secret
# Optional: if you do not want to store the raw password in Vercel,
# put a SHA-256 hex digest here instead of SITE_PASSWORD.
# SITE_PASSWORD_SHA256=
